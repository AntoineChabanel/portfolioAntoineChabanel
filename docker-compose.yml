version: '3.8'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: antoine_portfolio_app
        restart: always
        networks:
            - frontend

networks:
    frontend:
        external: true
        name: http_frontend